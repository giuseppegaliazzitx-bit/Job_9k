{
  "name": "job-9k",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first job application workstation. You paste apply URLs; it fills ATS forms and stops before submit.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w @job9k/api\" \"npm run dev -w @job9k/web\"",
    "dev:api": "npm run dev -w @job9k/api",
    "dev:web": "npm run dev -w @job9k/web",
    "test": "npm run test -w @job9k/core",
    "build": "npm run build --workspaces --if-present",
    "postinstall": "npx playwright install chromium"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
